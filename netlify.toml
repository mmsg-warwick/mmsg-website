[build]
  command = "hugo --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.147.0"  # replace with your version

[context.deploy-preview]
  command = "hugo --minify --buildFuture"
